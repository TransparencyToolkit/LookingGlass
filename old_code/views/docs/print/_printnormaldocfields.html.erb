  <% non_facet_check = Proc.new{ |f| checkIfX(f, @dataspec.doc_page_fields) &&
     !checkIfX(f, @dataspec.facet_fields)} %>
  
  <%= raw(sanitize_text(printData(@doc, non_facet_check, "fields", @dataspec,
      @dataspec.doc_page_fields))) %>
  
