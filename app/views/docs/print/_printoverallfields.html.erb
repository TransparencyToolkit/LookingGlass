<% non_item_check = Proc.new{ |f| checkIfX(f, @dataspec.doc_page_fields) &&
   !checkIfX(f, @dataspec.facet_fields) && !checkIfX(f, @dataspec.item_fields)} %>

<%= raw(sanitize_text(printData(d, non_item_check, "fields", @dataspec,
      @dataspec.doc_page_fields)))) %>

  
