<% item_check = Proc.new{ |f| checkIfX(f, @dataspec.item_fields) } %>
<% doc = d["_source"] ? d["_source"] : d %> <!-- Handle different doc formats -->
<%= raw(sanitize_text(printData(doc, item_check, "fields", @dataspec,
    @dataspec.item_fields))) %>
<hr>
