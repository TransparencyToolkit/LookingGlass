  <% non_facet_check = Proc.new{ |f| checkIfX(f, @dataspec.doc_page_fields) &&
     !checkIfX(f, @dataspec.facet_fields)} %>
  
  <%= raw(sanitize(printData(@doc, non_facet_check, "fields", @dataspec,
      @dataspec.doc_page_fields), tags: ['br', 'b', 'li', 'ul', 'ol', 'a', 'em', 'p'])) %>
  
