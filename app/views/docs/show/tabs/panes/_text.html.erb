<!-- Print out the overall thread fields for each doc -->
<%= render partial: 'docs/show/tabs/panes/text_fields', locals: {doc: @thread_fields } %>

<!-- Print out the item fields for each doc -->
<% @item_fields.each do |doc_item| %>
  <hr>
  <%= render partial: 'docs/show/tabs/panes/text_fields', locals: {doc: doc_item} %>
<% end %>
