<div class="detail-heading">Document Details</div>

<% first_doc = getFirstDoc %>
<% facet_check = Proc.new{ |f, doc| checkIfX(f, @facet_fields) && doc[f["Field Name"]] && !checkIfX(f, @item_fields)} %>

<%= raw(printData(first_doc, facet_check, "sidebar")) %>
	    
