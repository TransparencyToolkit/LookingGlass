<ul class="nav navbar-nav navbar-right">
    <% if ENV['WRITEABLE'] == "true" %>
    <li>
        <a href="/entities">
            Add Entity
        </a>
    </li>
    <li>
        <a href="<%= ENV['DOCUPLOAD_URL']+'/upload/'+ENV['PROJECT_INDEX']%>">
            Upload Documents
        </a>
    </li>
    <% else %>
    <% project["other_topbar_links"].to_h.each do |name, link| %>
    <li>
        <a href="<%= link%>"><%= name %></a>
      </li>
    <% end %>
    <% end %>
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            Info <span class="caret"></span>
        </a>
        <ul class="dropdown-menu" role="menu">
            <% project["info_links"].each do |name, link| %>
            <li><a href="<%= link %>" target="_blank"><%= name %></a></li>
            <% end %>
        </ul>
    </li>
</ul>
