<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title><%= content_for?(:title) ? yield(:title) : @instance.search_title %></title>

    <!--  Icons for Mobile Devices, Favicon -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="mobile-web-app-capable" content="yes" />

    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag @instance.theme + '/' + @instance.favicon, :rel => 'shortcut icon' %>

    <!-- Load in vars for use in javascript -->
    <%= render partial: 'layouts/javascriptvars' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">
          <% if @instance.logo != "" %><img src="/assets/<%= @instance.theme %>/<%= @instance.logo %>"><% end %>
          <span class="title"><%= @instance.search_title %> </span>
        </a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
       <%= render partial: 'layouts/searchbar' %> <!-- Render searchbar at top -->
       <%= render partial: 'layouts/linkmenu' %> <!-- Render info menu with links -->
      </div><!-- /.navbar-collapse -->
      </div>
    </nav>

    <%= bootstrap_flash %>

    <div class="container-fluid">
      <div class="row">
	    <%= yield %>
      </div>
    </div>

  </body>
</html>
